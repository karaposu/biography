# setup.py

from setuptools import setup, find_packages

setup(
    name='gitstory',
    version='0.0.0.1',
    packages=find_packages(),
     package_data={

    },
    install_requires=[



    ],
    author='Enes Kuzucu',
    # author_email='your.email@example.com',
    description='it is time for a git story',
    # Include other metadata as needed
)
